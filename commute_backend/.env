# Hosted Postgres (Neon) — used whenever DATABASE_URL is set
DATABASE_URL=postgresql://neondb_owner:npg_gSLPr2Hma6CR@ep-noisy-lake-b3zwmi48-pooler.c-4.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
DB_SSL=true

# API auth — must match the x-api-key the phone app sends
API_KEY=dev-secret-change-me
PORT=3000

# Local/Docker fallback (ignored while DATABASE_URL is set)
DB_HOST=localhost
DB_PORT=5432
DB_USER=commute
DB_PASSWORD=commute_pass
DB_NAME=commute